# UniRoute Backend Environment Variables
# Copy this file to .env and update the values

# Server Configuration
PORT=8084
ENV=development
# Ollama – use remote LLM server IP if backend runs elsewhere (see docs/REMOTE_OLLAMA_VLLM.md)
OLLAMA_BASE_URL=http://75.119.141.27:11434

# vLLM (OpenAI-compatible). Include /v1 in URL. Use same remote server as Ollama if applicable.
# On the host, start vLLM with the model you want, e.g.: vllm serve mistralai/Mistral-7B-Instruct-v0.2
# The Chat UI gets the model list from this server (GET /v1/models); ensure that model is running.
VLLM_BASE_URL=http://75.119.141.27:8001/v1
# VLLM_API_KEY=

# Security (CHANGE THESE IN PRODUCTION!)
# Generate secure secrets: openssl rand -hex 32
API_KEY_SECRET=change-me-in-production-min-32-characters-long
JWT_SECRET=change-me-in-production-jwt-secret-min-32-characters-long

# Database (PostgreSQL)
# Format: postgres://user:password@host:port/database?sslmode=disable
# For local: postgres://postgres:postgres@localhost:5432/uniroute?sslmode=disable
DATABASE_URL=

# Redis (Optional, for rate limiting and caching)
# Format: redis://localhost:6379
REDIS_URL=

# IP Whitelist (Optional, comma-separated)
# IP_WHITELIST=127.0.0.1,::1

# Phase 3: Cloud Provider API Keys (Optional, server-level fallback)
# Users can provide their own keys via BYOK feature
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# BYOK: Encryption Key for User Provider Keys
# Generate: openssl rand -hex 32
PROVIDER_KEY_ENCRYPTION_KEY=

# Base URL Configuration
# Base URL for the API server (used in documentation and examples)
# For local: http://localhost:8084
# For production: https://app.uniroute.co
BASE_URL=http://localhost:8084

# Tunnel Server Configuration
# Base domain for tunnel subdomains (e.g., "uniroute.co" or "yourdomain.com")
# If not set, defaults to "uniroute.co"
TUNNEL_BASE_DOMAIN=uniroute.co

# Localhost domain for local development tunnels
# Default: "localhost" (results in subdomain.localhost:port)
TUNNEL_LOCALHOST_DOMAIN=localhost

# Base port for TCP/UDP tunnel allocation
# Default: 20000 (tunnels will use ports starting from this number)
TUNNEL_TCP_PORT_BASE=20000

# Region label shown in CLI (where the tunnel server runs). E.g. "Europe (eu)" like ngrok.
# If unset, CLI shows "Local".
# TUNNEL_REGION=Europe (eu)

# Website URL for links in error pages and UI
# Default: https://uniroute.co
WEBSITE_URL=https://uniroute.co

# Seed admin (optional): at startup, ensure this user exists with admin role.
# Default: adikekizinho@gmail.com / Adike Kizito. Leave SEED_ADMIN_PASSWORD empty to auto-generate a strong password (printed to stderr once).
# SEED_ADMIN_EMAIL=adikekizinho@gmail.com
# SEED_ADMIN_NAME=Adike Kizito
# SEED_ADMIN_PASSWORD=
