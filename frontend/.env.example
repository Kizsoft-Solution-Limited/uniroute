# UniRoute Frontend Environment Variables
# Copy this file to .env and update the values

# API Configuration
# For local development, use http://localhost:8084 (gateway port)
# For production, use https://api.uniroute.dev
VITE_API_BASE_URL=http://localhost:8084

# Tunnel Server Configuration
# For local development, use http://localhost:8080
# For production, use https://tunnel.uniroute.dev
VITE_TUNNEL_SERVER_URL=http://localhost:8080

# Legacy API URL (optional, kept for compatibility)
VITE_API_URL=http://localhost:8084

# Note: DEV, PROD, and MODE are automatically set by Vite based on the build mode
# DEV=true when running `npm run dev`
# PROD=true when running `npm run build`
# MODE=development or production
